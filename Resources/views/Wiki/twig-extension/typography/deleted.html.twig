{# Resources/views/Wiki/twig-extension/typography/deleted.html.twig #}
{% extends "@Bootstrap/Wiki/layout.html.twig" %}
{% import "@Bootstrap/Wiki/macros.html.twig" as wiki %}
{% block bootstrapWikiContent %}
    {{ wiki.functionsTitle() }}
    {{ wiki.functionsList([
        "bootstrapDeleted",
    ]) }}
    <div class="panel panel-default">
        {{ wiki.functionTitle("bootstrapDeleted") }}
        <div class="panel-body">
            {{ wiki.argumentsList([
                ["string", "content", "the content"],
            ])}}
            {{ wiki.functionExample('{{ bootstrapDeleted({"content": "content"}) }}') }}
            {{ wiki.functionOutput(bootstrapDeleted({"content": "content"})) }}
        </div>
    </div>
{% endblock %}
