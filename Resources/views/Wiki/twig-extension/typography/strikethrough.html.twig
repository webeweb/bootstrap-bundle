{# Resources/views/Wiki/twig-extension/typography/strikethrough.html.twig #}
{% extends "@Bootstrap/Wiki/layout.html.twig" %}
{% import "@Bootstrap/Wiki/macros.html.twig" as wiki %}
{% block bootstrapWikiContent %}
    {{ wiki.functionsTitle() }}
    {{ wiki.functionsList([
        "bootstrapStrikethrough",
    ]) }}
    <div class="panel panel-default">
        {{ wiki.functionTitle("bootstrapStrikethrough") }}
        <div class="panel-body">
            {{ wiki.argumentsList([
                ["string", "content", "the content"],
            ])}}
            {{ wiki.functionExample('{{ bootstrapStrikethrough({"content": "content"}) }}') }}
            {{ wiki.functionOutput(bootstrapStrikethrough({"content": "content"})) }}
        </div>
    </div>
{% endblock %}
