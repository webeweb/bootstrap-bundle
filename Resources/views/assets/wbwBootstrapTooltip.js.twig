{# Resources/views/assets/wbwBootstrapTooltip.js.twig #}
/**
 * Bootstrap tooltip.
 */
class WBWBootstrapTooltip {

    /**
     * Refresh a tooltip.
     *
     * @param {jQuery} $el The element.
     * @return {undefined}
     */
    static refresh($el) {

        console.log({$el});

        $el.tooltip("destroy");
        $el.tooltip();
    }

    /**
     * Displays a tooltip.
     *
     * @param {jQuery} $el The element.
     * @return {undefined}
     */
    static tooltip($el) {

        console.log({$el});

        $el.tooltip();
    }

    /**
     * Displays the tooltips inside an element.
     *
     * @param {jQuery} $el The element.
     * @return {undefined}
     */
    static tooltips($el) {

        console.log({$el});

        WBWBootstrapTooltip.tooltip($el.find('[data-toggle="tooltip"]'));
    }
}
